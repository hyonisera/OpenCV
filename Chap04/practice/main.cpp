#include "opencv2/opencv.hpp"
#include <iostream>

using namespace cv;
using namespace std;

int main() {
	Mat board(500, 500, CV_8UC3, Scalar(0, 0, 0));

	setMouseCallback("paintBoard");
	if (event == EVENT_LBUTTONDOWN) {

	}
}